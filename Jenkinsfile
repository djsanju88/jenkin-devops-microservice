// SCRIPTED

//DECLARATIVE
pipeline {
     agent any
	 stages {
	     stage ('Build') {
		   steps {
	 echo "Build"
	}
   }
   stage ('Test') {
		   steps {
	 echo "Build"
	 	}
   }
   stage ('Intergration Test') {
		   steps {
	 echo "Build"
	 	}
   }
  }
}